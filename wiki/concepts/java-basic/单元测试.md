---
title: 单元测试
tags:
  - wiki
  - concept
  - java
  - java-basic
type: concept
source_series: Java-basic
status: seed
---

# 单元测试

[[wiki/concepts/java-basic/工程实践|返回工程实践]]

## 这一层回答什么问题

> JUnit 5 怎么用？Mockito 模拟了什么？测试应该怎么写才不是"补用例"？

单元测试不是写完代码再补的——它是设计工具。写测试的过程就是在检验你的接口是否可测、依赖是否清晰。

## JUnit 5

```java
@Test
@DisplayName("创建用户 - 邮箱已存在时应抛异常")
void createUser_WhenEmailExists_ShouldThrow() {
    // Given：准备数据和 mock
    when(userMapper.findByEmail("test@test.com")).thenReturn(existingUser);

    // When：执行
    assertThrows(BizException.class, () -> userService.create(dto));

    // Then：验证
    verify(userMapper, never()).insert(any());
}
```

核心注解：`@BeforeEach` / `@AfterEach`（每个测试前后）、`@BeforeAll` / `@AfterAll`（整个测试类前后，方法必须是 static）。

## Mockito：隔离依赖

`@Mock` 创建一个假对象，`@InjectMocks` 把 Mock 注入到被测试对象。`when(xxx).thenReturn(yyy)` 定义行为，`verify(xxx, times(n))` 验证调用。

`@Spy` 是部分 mock——真实对象部分方法被 mock。不要滥用 `@Spy`，它暗示你的类需要重构。

## 三层测试

```
单元测试（Service/Util） → 大量、快速、不依赖外部
集成测试（@SpringBootTest + DB） → 中等数量、验证集成
E2E 测试（全链路） → 少量、慢、验证完整流程
```

`@WebMvcTest` 只加载 Controller 层（MockMvc 模拟 HTTP），比 `@SpringBootTest` 快很多。`@DataJpaTest` 只加载数据层。

## 在系列里的位置

post 20。

## 推荐回看原文

- [[_posts/Java-basic/20-unit-testing|20-单元测试]]

## 相关概念

- [[wiki/concepts/java-basic/设计模式|设计模式]]
- [[wiki/concepts/java-basic/Git|Git]]
